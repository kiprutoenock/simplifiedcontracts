import 'package:flutter/material.dart';

class AppColorStyles {
  AppColorStyles._(); // prevent instantiation

  // 🌈 Brand colors
  static const Color primary = Color(0xFF1246AB);   // blue
  static const Color secondary = Color(0xFF43A047); // green
  static const Color tertiary = Color(0xFFFFA000);  // amber

  // 🖤 Neutrals
  static const Color background = Color(0xFFF7F7F7);
  static const Color surface = Colors.white;
  static const Color textPrimary = Colors.black;
  static const Color textSecondary = Colors.grey;
}